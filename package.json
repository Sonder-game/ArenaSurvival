{
  "name": "hero-survival",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w @hero-survival/server && (npm run dev -w @hero-survival/client & npm run start -w @hero-survival/server)",
    "build": "npm run build -ws"
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.5",
    "@types/uuid": "^10.0.0",
    "tsx": "^4.16.2",
    "typescript": "5.5.3"
  },
  "dependencies": {
    "uuid": "^13.0.0"
  }
}
