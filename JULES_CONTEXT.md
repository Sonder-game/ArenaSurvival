Стек: (Babylon.js, ECSY, Node.js, TypeScript)Клиент (Движок): $Babylon.js$.1Клиент (Язык): $TypeScript$.2Сервер (Среда): $Node.js$.3Сервер (Сеть): Библиотека $ws$ (чистые $WebSockets$).4Сборщик: $Vite$ (для $TypeScript$, $HMR$ и $tree-shaking$ $Babylon.js$).5Архитектура: (ECS, Авторитетный сервер, FSM)Архитектура клиента: $Entity Component System$ (ECS) с использованием $ECSY$ и $ecsy-babylon$. Это "code-first" паттерн, идеально подходящий для управления состояниями, баффами и способностями.7Архитектура сервера: Строго авторитетный сервер (Authoritative Server).11 Сервер владеет состоянием игры (здоровье, позиция, золото). Клиенты отправляют только намерения (ввод).12Сетевая модель: Клиент-Сервер через $WebSockets$.13Игровой цикл: Сервер работает на основе фиксированного "тика" (game loop, $setInterval$) для обработки ввода и рассылки состояния (snapshot/delta).11Управление состоянием (Клиент): Единая 3D-сцена ($single scene$).15 Для переключения между состояниями (Выбор героя, Волна, Магазин, Поражение) использовать $Finite State Machine$ (FSM), которая переключает оверлеи $Babylon.js$ $GUI$ ($AdvancedDynamicTexture$), а не меняет сцены.15Стиль Кода: (ECSY-паттерн, JSON-данные)ECSY-Паттерн: Вся игровая логика должна находиться в Системах (Systems). Все данные и состояние — в Компонентах (Components). Компоненты не должны содержать методов.8Данные: Все игровые определения (герои, способности, предметы, волны) должны быть вынесены в $JSON$-файлы и загружаться при старте.18Типизация: Использовать строгую типизацию $TypeScript$.3Асинхронность: Использовать $async/await$ для загрузки ассетов (например, $ImportMeshAsync$).21Движение ИИ: Для ИИ врагов использовать $Babylon.js$ $RecastJSPlugin$ (NavMesh) для поиска пути.22⛔️ Запреты (Deny List): (Ключевые оптимизации и ограничения)КРИТИЧЕСКИ: Не использовать $WebRTC$ (P2P). Это избыточно сложно для данной задачи.13КРИТИЧЕСКИ: Не использовать полный физический движок (Havok/Ammo) для движения игрока. Это медленно и не нужно для top-down игры. Использовать $mesh.moveWithCollisions()$.24КРИТИЧЕСКИ: Не использовать $scene.debugLayer.show()$ в $production$-сборке.27КРИТИЧЕСКИ: Не создавать дубликаты мешей для "толп" врагов (волн). Использовать $Thin Instances$ для рендеринга.27Не использовать несколько сцен $Babylon.js$ или $scene.dispose()$. Управлять состоянием через FSM и GUI-оверлеи.15Не использовать $Socket.io$. Использовать чистую библиотеку $ws$ для максимального контроля.4Не устанавливать $npm$-зависимости, не указанные в плане.